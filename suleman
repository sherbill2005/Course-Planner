Tasks (Phase 2 - Course Management):

Next steps:
1. Update the Dashboard to fetch "Available Courses" from a new API (to be created by Nasir/Huzaifa).
2. Implement "Enroll" and "Remove" buttons that call backend APIs instead of local state.
3. Replace all remaining `localStorage` mock logic with actual state from the backend.
4. Add better feedback (toasts/notifications) when a course is successfully added or a clash is detected.

Important:
- Coordinate with Huzaifa on the API endpoint names.
- Ensure the UI handles "Loading" states while waiting for the Cloud DB.
