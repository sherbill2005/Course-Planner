Tasks (updated after Huzaifa auth commit c54b6d4):

Current context:
- Login and registration pages and APIs are now available.
- Frontend should now shift from static flows to API-driven authenticated flow.

Next steps:
1. Integrate login/register UI with backend responses and consistent error states.
2. Add protected-route behavior for dashboard/course pages.
3. Build course listing and selection UI from `/api/courses` (once Nasir finalizes).
4. Add loading, empty, and failure states for all data-fetching screens.
5. Align local user state with real backend auth/session approach.

Weak points to address from latest commit:
- Mixed auth patterns exist (API + localStorage context); this can cause inconsistent UX.
- No token/session persistence strategy is finalized yet.
- Error handling UX is still basic.

Important configuration to consider:
- Tech stack: **Next.js App Router, React, TypeScript, Tailwind CSS, Fetch API**
- Validate environment-based API assumptions for local/dev runs.
- Keep request/response shapes synchronized with backend contracts.
- Coordinate route protection rules with Huzaifa and Nasir.

Phase 1 expected output:
- Working UI showing courses
- Auth-aware course selection flow
- Stable frontend integration with backend APIs
