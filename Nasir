Tasks (Phase 2 - Course Management):

Next steps:
1. Update `schema.prisma` to include the `Course` model (referencing `lib/types.ts`).
2. Create a `prisma/seed.ts` file to populate the Neon database with the courses currently in `lib/dummyData.ts`.
3. Goal: Make sure everyone is pulling course data from the DB instead of a local JS file.
4. Ensure the database schema supports the link between Users and their Enrolled Courses.

Important:
- Coordinate with Huzaifa on the relation between User and Course models.
- Coordinate with Zaid on the time/session format to ensure scheduling logic works.
