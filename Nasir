Tasks (updated after Huzaifa auth commit c54b6d4):

Current context:
- Auth APIs now exist (`/api/register`, `/api/login`) with PostgreSQL + Prisma user table.
- Backend foundations are now stronger, so your next APIs should be user-aware.

Next steps:
1. Add course and schedule Prisma models with user linkage (owner/student relation).
2. Implement `/api/courses` CRUD with clean DTOs and validation.
3. Implement `/api/schedule` endpoints for add/remove/list selected courses.
4. Ensure APIs can identify the logged-in user context (once auth token/session is introduced).
5. Provide stable response shapes for frontend and scheduler integration.

Weak points to address from latest commit:
- Auth context and backend are not fully integrated yet.
- No authorization checks exist for course/schedule endpoints yet.
- Input normalization/validation is still minimal.

Important configuration to consider:
- Tech stack: **Next.js API Routes, TypeScript, Prisma, PostgreSQL**
- Ensure `DATABASE_URL` is set in the active backend module `.env` (currently `./Huzaifa/.env`).
- Apply Prisma migrations before API tests.
- Keep route contracts documented and version-stable for Suleman + Zaid.

Phase 1 expected output:
- Running backend server
- APIs returning course + schedule data from DB
- User-linked data flow ready for frontend and scheduling module
